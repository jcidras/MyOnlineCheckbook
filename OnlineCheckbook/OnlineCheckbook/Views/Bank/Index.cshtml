@model OnlineCheckbook.Models.Bank

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    @Html.ActionLink("Back to Profile", "Profile", "Home", new { id = Model.UserID }, new { @class = "btn btn-default" })
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.Label("Bank Name")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.BankName)
        </dd>
        
    </dl>
    
    @Html.ActionLink("Add Account", "Create", "Account", new { id = Model.BankID }, new { @class = "btn btn-default" })

    <h4>Accounts</h4>
    <table class="table">
        <tr>
            <th>
                Name
            </th>
            <th>
                Current Balance
            </th>
            <th></th>
            <th></th>                
        </tr>
        @if (Model.Accounts.Count == 0)
        { 
            <tr>
                <td colspan="4">
                    You have no accounts. Please add one!
                </td>
            </tr>
        }
        else
        { 
            foreach (var account in Model.Accounts)
            {
                <tr>
                    <td>
                        @Html.ActionLink(account.Name, "Index", "Account", new { id = account.Id }, new { @style = "text-decoration:none;" })
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => account.CurrentBalance)
                    </td>
                    <td>
                        @Html.ActionLink("Delete", "Delete", "Account", new { id = account.Id, bankId = account.BankId }, new { @class = "btn btn-danger" })
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", "Account", new { id = account.Id }, new { @class = "btn btn-default" })
                    </td>
                </tr>
            }
        }            
    </table>    
</div>

